#ifndef DEF_PERSONNAGE
#define DEF_PERSONNAGE

class Personnage
{
public:
    
    Personnage() = default;
    
    ~Personnage() = default;
    
    void SePresenter() const;
        
};

# include "Personnage.hxx"

#endif
